<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StudentApp.Movile.Views.Login.Login"
             BindingContext="{Binding Login, Source={StaticResource Locator}}">

  <StackLayout VerticalOptions="Center">
    <Label Text="{Binding TextWelcome}" VerticalOptions="Center" TextColor="Blue" FontSize="25"> </Label>
    <Entry Text="{Binding LogingIn.User, Mode=TwoWay}" Placeholder="{Binding phUser}"/>
    <Entry Text="{Binding LogingIn.Password, Mode=TwoWay}" Placeholder="{Binding phPass}" IsPassword="true" />
    <Button Command="{Binding CmdLogin}" Text="{Binding nmBtnLogin}" />
    <Button Text="Facebook" BorderColor="Blue" />
    <Button Text="Gmail" BorderColor="Red" />
    <Label Text="{Binding Lk, Mode=TwoWay}" VerticalOptions="Center" TextColor="Blue" FontSize="25"> </Label>
  </StackLayout>
</ContentPage>

